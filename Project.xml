<?xml version="1.0" encoding="utf-8"?>
<project xmlns="http://lime.openfl.org/project/1.0.4"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://lime.openfl.org/project/1.0.4 http://lime.openfl.org/xsd/project-1.0.4.xsd">

	<!-- _________________________ Application Settings _________________________ -->

	<app title="Dreamland" file="dreamland" main="Main" version="1.0.0" company="Sinco" />
	<app title="Dreamland (debug)" if="debug"/>

	<!--The
	flixel preloader is not accurate in Chrome. You can use it regularly if you embed the swf
	into a html file
		or you can set the actual size of your file manually at "FlxPreloaderBase-onUpdate-bytesTotal"-->
	<app preloader="flixel.system.FlxPreloader" />

	<!--Minimum
	without FLX_NO_GAMEPAD: 11.8, without FLX_NO_NATIVE_CURSOR: 11.2-->
	<set name="SWF_VERSION" value="11.8" />

	<define name="MOBILE_BUILD_TESTING" if="android_testing"/>
	
	<define name="MOBILE_BUILD" if="android"/>
	<define name="MOBILE_BUILD" if="MOBILE_BUILD_TESTING" unless="MOBILE_BUILD"/>

	<!-- ____________________________ Window Settings ___________________________ -->

	<!--These
	window settings apply to all targets-->
	<window width="640" height="480" fps="144" background="#000000" hardware="true" vsync="false" />

	<!--HTML5-specific-->
	<window if="html5" resizable="true" />

	<!--Desktop-specific-->
	<window if="desktop" orientation="landscape" fullscreen="false" resizable="true" />

	<!--Mobile-specific-->
	<window if="MOBILE_BUILD" orientation="landscape" fullscreen="true" width="0" height="0" unless="MOBILE_BUILD_TESTING"/>
	
	<window height="720" width="1280" orientation="landscape" if="MOBILE_BUILD_TESTING"/>

	<!-- _____________________________ Path Settings ____________________________ -->

	<set name="BUILD_DIR" value="export" />
	<set name="BUILD_DIR" value="export-mobile" if="MOBILE_BUILD" />

	<set name="BUILD_DIR" value="${BUILD_DIR}/debug" if="debug" />
	<set name="BUILD_DIR" value="${BUILD_DIR}/release" unless="debug" />

	<source path="source" />
	<assets path="assets" exclude="assets/exclude/"/>
	<assets path="exclusives/mobile" rename="assets/mobile" if="MOBILE_BUILD"/>

	<define name="DISCORDRPC" if="windows" unless="hl || neko || MOBILE_BUILD" />
	<define name="POLYMOD_MODDING" unless="html5 || neko || MOBILE_BUILD" />
	<define name="BLOCK_SOME_POLYMOD_TRACES" if="blockSomePolymodTraces" />

	<section if="POLYMOD_MODDING">
		<define name="SCRIPT_FILES" />
		<define name="ASSET_REPLACEMENT" />
		<assets path="example_mods" rename="mods" />
		<assets path="debug_mods" rename="mods" if="debug" />
	</section>
	
	<define name="IRIS_DEBUG"/>
	<define name="hscriptPos" if="IRIS_DEBUG"/>


	<!-- _______________________________ Libraries ______________________________ -->

	<haxelib name="flixel" />
	<haxelib name="flixel-addons" />

	<haxelib name="discord_rpc" if="DISCORDRPC" />

	<haxelib name="polymod" if="POLYMOD_MODDING" />
	<haxelib name="hscript-iris" />

	<!-- Options for Polymod -->
	<section if="polymod">
		<!-- Turns on additional debug logging. -->
		<haxedef name="POLYMOD_DEBUG" value="true" if="debug" />
		<!-- The file extension to use for script files. -->
		<haxedef name="POLYMOD_SCRIPT_EXT" value=".hxc" />
		<!-- Which asset library to use for scripts. -->
		<haxedef name="POLYMOD_SCRIPT_LIBRARY" value="scripts" />
		<!-- The base path from which scripts should be accessed. -->
		<haxedef name="POLYMOD_ROOT_PATH" value="scripts/" />
		<!-- Determines the subdirectory of the mod folder used for file appending. -->
		<haxedef name="POLYMOD_APPEND_FOLDER" value="_append" />
		<!-- Determines the subdirectory of the mod folder used for file merges. -->
		<haxedef name="POLYMOD_MERGE_FOLDER" value="_merge" />
		<!-- Determines the file in the mod folder used for metadata. -->
		<haxedef name="POLYMOD_MOD_METADATA_FILE" value="_polymod_meta.json" />
		<!-- Determines the file in the mod folder used for the icon. -->
		<haxedef name="POLYMOD_MOD_ICON_FILE" value="_polymod_icon.png" />
	</section>

	<!--In
	case you want to use the addons package-->
	<!--<haxelib
	name="flixel-addons" />-->

	<!--In
	case you want to use the ui package-->
	<!--<haxelib
	name="flixel-ui" />-->

	<!--In
	case you want to use nape with flixel-->
	<!--<haxelib
	name="nape-haxe4" />-->

	<!-- ______________________________ Haxedefines _____________________________ -->
	<!-- HScript relies heavily on Reflection, which means we can't use DCE. -->
	<haxeflag name="-dce no" />
	<!-- Something about stack traces-->
	<haxedef name="hscriptPos" />

	<!--Remove
	the legacy health system-->
	<haxedef name="FLX_NO_HEALTH" />

	<!--Enable
	the Flixel core recording system-->
	<!--<haxedef
	name="FLX_RECORD" />-->

	<!--Disable
	the right and middle mouse buttons-->
	<!--<haxedef
	name="FLX_NO_MOUSE_ADVANCED" />-->

	<!--Disable
	the native cursor API on Flash-->
	<!--<haxedef
	name="FLX_NO_NATIVE_CURSOR" />-->

	<!--Optimise
	inputs, be careful you will get null errors if you don't use conditionals in your game-->
	<!-- <haxedef name="FLX_NO_MOUSE" if="mobile" /> -->
	<!-- <haxedef name="FLX_NO_KEYBOARD" if="mobile" /> -->
	<haxedef name="FLX_NO_TOUCH" if="desktop" />
	<!--<haxedef
	name="FLX_NO_GAMEPAD" />-->

	<!--Disable
	the Flixel core sound tray-->
	<!--<haxedef
	name="FLX_NO_SOUND_TRAY" />-->

	<!--Disable
	the Flixel sound management code-->
	<!--<haxedef
	name="FLX_NO_SOUND_SYSTEM" />-->

	<!--Disable
	the Flixel core focus lost screen-->
	<!--<haxedef
	name="FLX_NO_FOCUS_LOST_SCREEN" />-->

	<!--Disable
	the Flixel core debugger. Automatically gets set whenever you compile in release mode!-->
	<haxedef name="FLX_NO_DEBUG" unless="debug" />

	<!--Enable
	this for Nape release builds for a serious peformance improvement-->
	<haxedef name="NAPE_RELEASE_BUILD" unless="debug" />

	<!-- Haxe 4.3.0+: Enable pretty syntax errors and stuff. -->
	<!-- pretty (haxeflixel default), indent, classic (haxe compiler default) -->
	<haxedef name="message.reporting" value="pretty" />

	<!-- _________________________________ Custom _______________________________ -->

	<!--Place
	custom nodes like icons here-->
	<icon path=".dev/icon.png" unless="MOBILE_BUILD" />
	<icon path=".dev/mobileIcon.png" if="MOBILE_BUILD" />
</project>